# PROTOTYPE — throwaway. Answers wayfinder #33: whether Rust survives Grind's awkward core.
# Not the base. Nothing here is a translation source for the real rewrite.
[workspace]
resolver = "2"
members = ["observed", "transcript", "supervise", "runlock", "record"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
