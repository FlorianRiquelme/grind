.DS_Store

# Session handoff batons — per-session, per-worktree, never shared history
.handoff.md
.handoff.consumed.md

# Local tool caches
.impeccable/

# Run state — the supervisor's own working record, never committed
.grind/

# Python bytecode from loading bin/grind as a module in tests
__pycache__/
