.DS_Store

# Session handoff batons — per-session, per-worktree, never shared history
.handoff.md
.handoff.consumed.md

# Local tool caches
.impeccable/

# Cargo build output
/target

# Working plans — session-local, never repo history
docs/plans/
